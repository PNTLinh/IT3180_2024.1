//checkbox_style
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ControlTemplate x:Key="CheckBoxQuyDinh" TargetType="{x:Type CheckBox}">
        <Grid Background="{TemplateBinding Background}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="35"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <CheckBox Height="Auto" Margin="5,10,5,0" IsChecked="{TemplateBinding IsChecked}" VerticalAlignment="Top" HorizontalAlignment="Center" BorderBrush="{TemplateBinding BorderBrush}" Foreground="{TemplateBinding Foreground}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
            <TextBlock Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" TextWrapping="Wrap" />
        </Grid>
    </ControlTemplate>
</ResourceDictionary>